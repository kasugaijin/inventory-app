<p style="color: green"><%= notice %></p>

<h1>Warehouses</h1>

<div id="warehouses">
  <% @warehouses.each do |warehouse| %>
    <%= warehouse.name %>
    <p>
      <%= link_to "Show warehouse", warehouse %>
    </p>
  <% end %>
</div>

<%= link_to "Create New Warehouse", new_warehouse_path %>
